{
  "name": "@animalsurvivor/run-director",
  "version": "0.1.0",
  "private": true,
  "description": "Renderer-independent deterministic run/encounter director for AnimalSurvivor. Turns fixed simulation ticks and gameplay metrics into deterministic encounter commands. Zero runtime dependencies.",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node --test \"dist/test/*.test.js\"",
    "lint": "node scripts/lint.mjs",
    "bench": "tsc -p tsconfig.json && node dist/bench/bench.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
